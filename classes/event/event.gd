extends Resource
class_name Event

var y:float

func is_valid() -> bool:
	assert(false, "Please override `is_valid()` in the derived script.")
	return false

func value():
	assert(false, "Please override `value()` in the derived script.")
